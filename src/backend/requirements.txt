# Requirements for Railway deployment
# Excludes: qdrant-client, sentence-transformers (heavy ML models)

# Core LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0

# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=14.0

# Database (lightweight - for session management)
sqlalchemy>=2.0.0
asyncpg>=0.30.0
greenlet>=3.0.0

# Utilities
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
httpx>=0.28.0
pyjwt>=2.8.0

# Monitoring
structlog>=24.4.0

# Note: RAG disabled (no qdrant-client/sentence-transformers)
# Note: PostgreSQL driver (asyncpg) disabled - use SQLite in-memory
