# Lightweight requirements for Railway deployment
# Excludes heavy ML dependencies (chromadb, sentence-transformers)

# Core LangChain (without heavy embeddings)
langgraph>=0.2.0
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0

# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=14.0

# Utilities
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
httpx>=0.28.0
pyjwt>=2.8.0

# Monitoring (lightweight)
structlog>=24.4.0

# Note: RAG features disabled (no chromadb/sentence-transformers)
# Note: Database features disabled (no sqlalchemy/asyncpg)
# Note: Redis disabled
