apiVersion: v1
kind: Service
metadata:
  name: aos-backend
  namespace: aos
  labels:
    app.kubernetes.io/name: aos
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: aos
    app.kubernetes.io/component: backend
