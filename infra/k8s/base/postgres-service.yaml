apiVersion: v1
kind: Service
metadata:
  name: aos-postgres
  namespace: aos
  labels:
    app.kubernetes.io/name: aos
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app.kubernetes.io/name: aos
    app.kubernetes.io/component: postgres
