apiVersion: v1
kind: Service
metadata:
  name: aos-redis
  namespace: aos
  labels:
    app.kubernetes.io/name: aos
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    app.kubernetes.io/name: aos
    app.kubernetes.io/component: redis
