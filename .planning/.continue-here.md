---
phase: adhoc (skill-security-audit → Phase A fail-closed 전환)
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-08-03T15:11:34.157Z
---

<current_state>
Task 1·2는 완료·머지됨(PR #231, `af29e7f`). Task 3(Phase A 승인 게이트 fail-closed 전환)이
미커밋 3파일로 남아 있고, Codex 6차 지적을 **의도적으로 미반영**한 채 중단했다.

워킹트리는 5차 검증까지 반영된 상태로 보존돼 있다 — 처음부터 다시 할 필요 없다.
</current_state>

<completed_work>

- Task 1: 스킬 보안 스캔 — Done (`af29e7f`)
  - `.claude/skills` 11개 + `.agents/skills` 22개 + `.claude/commands` 18개(SKILL.md 래핑) 스캔
  - 정적 고유 14건 전부 소스 대조로 오탐 판정 (`grep -v` 제외필터, 로컬 npx 바이너리, detach용 nohup 등)
  - LLM 의미 계층(`SKILLSPECTOR_PROVIDER=claude_cli`, 로컬 인증·과금 없음): 6건 기각(수동 판정과 일치), 3건 신규 탐지
- Task 2: baseline 7종 + PR #231 — Done (`af29e7f`, CI 9/9 pass)
  - 재귀 스캔 `--baseline` 미지원을 우회해 스킬별 생성, Red-Green 검증 7/7 → 0건
- Task 3: Phase A fail-closed 전환 — In progress
  - 정책 본문·승인 재개 규칙·Phase 0 분기 재배치 완료 (Codex 5라운드 반영)
</completed_work>

<remaining_work>

- Task 3.1: `SKILL.md` Phase 0 "[최우선] 존재 + Phase A가 BLOCKED" 분기에 **승인 없는 순수 수정 요청**
  케이스 추가 (예: "마이그레이션 빼고 다시 짜줘" → Phase A 재실행). 현재 어느 하위 케이스에도 안 맞고
  first-match라 아래 분기로도 못 내려가 **영구 BLOCKED**
- Task 3.2: 같은 분기에 **취소/포기 + 무관한 새 기능** 케이스 추가 (`_workspace/` 타임스탬프 아카이브 후
  초기 실행). 현재는 최우선 분기가 가로채 "새 기능" 분기에 도달 불가 → stale 워크스페이스를 버릴 수 없음
- Task 3.3: Codex 재검증 → 통과 시 **3파일만** 커밋(`install.sh` 제외) → 브랜치 → PR
</remaining_work>

<decisions_made>

- Phase A AFK 정책을 **「명시적 위임 토큰」**으로 전환 — 자기 판정 2개 중 "위임 여부 추론"을 제거.
  사용자가 3개 옵션 중 선택(①AFK 완전 제거는 기능 상실, ③독립 위험 판정은 런당 +1 에이전트)
- **타이머 제거** — "~5분 무응답 후 BLOCKED"는 턴이 끝나면 전환할 주체가 없어 실행 불가.
  승인 요청 시점에 즉시 `BLOCKED`(사유=승인 대기) 기록 후 턴 종료로 변경 (Codex 3차 P1)
- baseline은 **`--no-llm`으로 생성** — 정적 오탐만 억제하고 LLM 의미 findings(Phase A 지적)는
  표면에 남기기 위함. fingerprint가 스킬 전체 콘텐츠 해시 기반이라 스킬 수정 시 억제가 풀림(fail-closed, 실측)
- `docs/harness-reliability-2026-07.md`는 **이력 보존 + 개정 표기** — 과거 시점 기록이라 사실을 고치지 않음
</decisions_made>

<blockers>

- **Codex 6라운드가 전부 직전 수정의 부작용을 지적** — 연속 판단으로 수렴하지 않아 중단.
  순서: 출구 미정의 → 출구의 결함 → 상태 영속화 주체 부재(P1) → 분기 선점(P1) → 조건 도달 불가 →
  선점의 반작용(P1). 4차 수정이 5차·6차에서 정반대 방향의 문제를 각각 만들었다.
  **회피책**: 7차에도 P1이 나오면 문서 분기 나열이 아니라 **상태 전이표**(BLOCKED 사유 × 입력 유형 → 행동)로
  재작성할 것 — 누락을 구조적으로 막는 형태
- 컨텍스트 예산 초과 — 새 세션에서 재개
</blockers>

<context>
원 지적은 SkillSpector LLM 의미 분석이 잡았다: 이 하네스의 다른 게이트는 전부 fail-closed인데
(에이전트 2회 실패·Phase C FAIL·SKIPPED 우회 금지·모호하면 고위험) **Phase A 승인만 fail-open**이었다.
무응답은 정보 부재인데 승인으로 해석했다. 이는 외부 보안 의견이 아니라 **문서 자신의 내적 모순**이다.

교훈: 문서 기반 정책도 **상태 기계**다. 한 전이의 기본값만 바꿔도 그 상태의 생애주기 전체
(출구·영속화 시점·다른 원인과의 충돌·분기 우선순위·조건 도달성·선점 부작용)를 다시 검토해야 한다.

**원 지적 자체는 유효하므로 폐기하지 말 것.** 남은 건 분기 완전성 확보뿐이다.

Codex 로그: scratchpad `codex-review{,2,3,4,5,6}.log`
스캔 리포트: scratchpad `skillspector-report.md` (세션 종료 시 휘발 — 필요하면 먼저 회수)
</context>

<next_action>
Start with: `.claude/skills/aos-feature-harness/SKILL.md` Phase 0의 "[최우선] 존재 + Phase A가 BLOCKED"
분기를 열고, 하위 케이스에 (4) 승인 없는 순수 수정 요청 → Phase A 재실행, (5) 취소/포기 + 새 기능 →
워크스페이스 아카이브 후 초기 실행 두 가지를 추가한 뒤 Codex 재검증
(`nohup node ~/.claude/plugins/cache/openai-codex/codex/*/scripts/codex-companion.mjs review --scope working-tree`).
</next_action>
